'use strict';

/**
 * 获取错误描述
 * @param res
 */
const getErrorDesc = (res) => {
    var _a;
    const helps = (_a = res.data.error) === null || _a === void 0 ? void 0 : _a.helps;
    if (helps === null || helps === void 0 ? void 0 : helps.length) {
        return helps.map((h) => h.description).join('\n');
    }
    return res.data.msg;
};

exports.getErrorDesc = getErrorDesc;
//# sourceMappingURL=utils.js.map
